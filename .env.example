# Proxy Authentication
# Password required from downstream clients connecting to this proxy
VPS_PASSWORD=my_vps_password

# Upstream Provider Credentials
NETLOX_PASSWORD=your_netlox_password
MASKIFY_PASSWORD=your_maskify_password
